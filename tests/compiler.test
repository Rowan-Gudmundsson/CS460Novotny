void blah();
void blah(int);
void blah(char);

int something = 2;

/* struct T {
	int x;
	float y;
}; */

int main() {
	// This is a test
	char z = 's';
	short x = 0 , y = 2;
	int q[50 + 2];
	int m[2][5];
	int n[3+2][3*2][4];
	int o[2 * 1][2 / 1][2 << 0][2 >> 0][2];

	// struct T t;

	something = 3;
	/* test */
	{
		/* This
		   Is
		   A
		   Multiline
		   Comment */ int x;
	}

	x = x + y / 2;
	y *= 3 % z;


	if(x == 2 || y > z / 2) {
		z = 3;
	} else if (1) {
		y = 3;
	} else {
		x = 2;
	}

	if(x % y > q[y + z]) {
		q[x * y] = 42;
	}

	m[2][3] = 3;
	n[x+y][2][z] = 4;
	o[0][1][x][0][1] = o[1][1][x][y][1];

	for(;x < 2;) {
		q[x] = y * x;
	}

	blah(4);
	blah('4');
	blah();
}

void blah(int y) {
	int x = y * 2;
	blah(x);
}

void blah(char y) {
	char x = 0;
}
